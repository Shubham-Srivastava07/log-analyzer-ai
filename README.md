ai-log-analyzer/
├── app.py           # Streamlit app entry point
├── parser.py        # Logic to parse different log files
├── analyzer.py      # Logic to detect patterns
├── ui.py            # Dashboard UI elements
├── requirements.txt # Dependencies (streamlit, etc.)
└── sample_logs/     # Raw and Error logs
    ├── nginx.log
    ├── nginx_error.log
    ├── jenkins.log
    ├── jenkins_failure.log
    ├── app.log
    └── app_exception.log
